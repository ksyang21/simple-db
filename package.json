{
  "name": "db-viewer",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w client\"",
    "dev:app": "tauri dev",
    "build": "npm run build -w client && npm run build -w server",
    "build:app": "CARGO_TARGET_DIR=$PWD/src-tauri/target tauri build",
    "start": "NODE_ENV=production PORT=8080 npm run start -w server"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.2",
    "concurrently": "^9.1.2"
  }
}
